apiVersion: v1
kind: Service
metadata:
  name: rwx-test-kadalu
  labels:
    app: rwx-test-kadalu
spec:
  ports:
    - port: 80
  selector:
    app: rwx-test-kadalu
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rwx-test-kadalu
  labels:
    app: rwx-test-kadalu
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: rwx-test-kadalu
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: rwx-test-kadalu
    spec:
      containers:
        - image: ubuntu:xenial
          imagePullPolicy: IfNotPresent
          command: [ "/bin/sh", "-c" ]
          args:
            - sleep 10; touch /data/index.html; while true; do date >> /data/index.html; sleep 1; done;
          name: rwx-test-kadalu
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /data
              name: rwx-test-kadalu
        - image: nginx:stable
          imagePullPolicy: IfNotPresent
          name: nginx
          ports:
            - containerPort: 80
              name: http
          volumeMounts:
            - mountPath: /usr/share/nginx/html
              name: rwx-test-kadalu
      restartPolicy: Always
      volumes:
        - name: rwx-test-kadalu
          persistentVolumeClaim:
            claimName: rwx-test-kadalu